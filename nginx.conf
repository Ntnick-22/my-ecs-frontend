server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass http://learn-ecs-backend.internal:8000;
    }

    location / {
        try_files $uri /index.html;
    }
}
